{
  "name": "root",
  "private": true,
  "devDependencies": {
    "husky": "^1.3.1",
    "lerna": "^3.13.2",
    "lint-staged": "^8.1.5",
    "prettier": "^1.17.0"
  },
  "scripts": {
    "start": "npm -C ./packages/myk-app run start",
    "build": "npm -C ./packages/myk-app run build",
    "test": "npm -C ./packages/myk-app run test"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,ts,jsx,tsx,json,css,md}": [
      "prettier --write",
      "git add"
    ]
  }
}
